# ESP32 编译验证

仅编译，不烧录。用于快速验证代码修改是否有编译错误。

## 执行步骤

运行编译命令：
```bash
zsh -ic 'idf55 && idf.py build' 2>&1 | tail -30
```

如果编译失败：
- 阅读错误信息，定位问题文件和行号
- 修复编译错误
- 重新编译
- 最多重试 2 次

编译成功后报告：
- 固件大小和剩余空间百分比
- 是否有 warning 需要关注

# mydazy-p30 扩展功能清单（给开发核对）

> **基线**：fork 自 78/xiaozhi-esp32 的 **v1.9.x（2025）**，默认板型 **P30-4G**；78 官方现已发展到 **v2.2.6（2026-06）**，领先我们一年多、跨 v2.0/v2.1/v2.2 多个大版本。
> **盘点方式**：3 个 agent 逐文件读真实代码 + 与 `xiaozhi/main` 逐项 diff 核对。
> **日期**：2026-06-17　**配套**：交互流程见 `docs/交互功能流程_2026-06-17.md`

## 图例
- 🆕 纯新增（78 官方完全没有）　🔧 改动/重构官方　📦 仅 vendor 官方组件（未改源码）　⚠️ 已就位未接线/技术债

---

## 一、硬件 / 板级 / 驱动

| 功能 | 类型 | 代码位置 | 说明 |
|---|---|---|---|
| **P30-4G 板**（WiFi+4G 双网 `DualNetworkBoard`） | 🆕 | `main/boards/mydazy-p30-4g/mydazy_p30_board.cc:129` | 官方 boards 下无任何 mydazy 板 |
| **P30-WiFi 板** | 🆕 | `main/boards/mydazy-p30-wifi/mydazy_p30_board.cc:127` | 与 4G 板共用 Init 骨架，去掉 4G |
| 硬件组合（JD9853 屏 / ES8311+ES7210 / AXS5106L / SC7A20H / ML307R，**共用 GPIO9 总电源**） | 🆕 | `config.h:30`、board `:657/:962` | 断电即硬复位音频/4G/LCD |
| `board.h` 加 7 个自研虚函数（GetBrandName/EnterDeepSleep/EnableAutoSleep/CanSwitchNetwork/ToggleAecMode…） | 🔧 | `main/boards/common/board.h:85-91` | 深睡/双网/AEC 产品接口 |
| **SC7A20H 加速度计驱动**（摇一摇/桌面双击/EXT1 拿起唤醒，寄存器级自写） | 🆕 | `components/mydazy__esp_sc7a20h/sc7a20h.c` | 官方对此芯片 0 引用 |
| **流式 MP3 播放器**（HTTP 分块→PSRAM ring→解码→I2S，断点续传/暂停/打断） | 🆕 | `components/mydazy__esp_mp3_player/mp3_player.cc` | 官方只有 Opus 对话流 |
| **I2C 总线串行化 worker**（会话锁/连错自动 bus_reset/诊断统计） | 🆕 | `components/mydazy__i2c_bus_worker/i2c_bus_worker.c` | 为 4G RF 共线下抢总线而建 |
| codec I2C 控制转发层（ES8311/ES7210 访问全经 worker 串行） | 🔧 | `components/mydazy__codec_ctrl_i2c/...:134` | 官方直用原生，无串行化 |
| **AXS5106L 触摸全套**（坐标+LVGL+**手势引擎** on_wake/click/double/swipe/long_press） | 🆕 | `components/esp_lcd_touch_axs5106l/axs5106l_touch.c` | 官方触摸无手势识别 |
| **触摸 4G 射频抗扰层**（INT storm 检测 / RF 双档 NORMAL·STRICT / 中值滤波 / 息屏首触吞按压） | 🆕 | `axs5106l_touch.c:56-76`、board `:328/:330` | 对应「触摸乱跳=4G 射频耦合」缓解 |
| **触摸芯片固件 OTA**（版本比对→debug-mode 全片 MTP 重刷） | 🆕 | `axs5106l_upgrade.c:158-268` | 官方触摸驱动无刷写能力 |
| 电池 ADC 监测+电量分档（充/放两套表+eFuse 校准） | 🔧 | `main/boards/common/power_manager.h` | 官方仅个别板有，我们搬到 common 改造 |
| **运行期过放保护**（08-P0-A 头号红线：<3400mV×3 关机；充电复位；未校准回退保守 Vref+产线登记） | 🆕 | `power_manager.h:215-226/:273/:343` | 官方 power_manager 无此项 |
| **深睡 + 三源唤醒**（EXT0 开机键 / EXT1 陀螺仪拿起 / TIMER 闹钟，按 wakeup_cause 分流） | 🆕 | board `:193/:218/:573` | 官方无 EXT1 陀螺仪唤醒 |
| `PowerSaveTimer` 深省电（降频<120M 关音频输入 / atomic 防撕裂 / IsInSleepMode） | 🔧 | `power_save_timer.cc/.h` | 官方裸 bool 有 data race |
| **自研 UI 系统 `UiDisplay`**（时钟/聊天/音乐页/番茄钟页/二维码/开机动画/状态栏/关于） | 🆕 | `main/display/ui_display.cc`、`scene_type.h` | 官方止步表情+气泡+状态栏 |
| **控制中心**（iOS 风 3×2 宫格） | 🆕 | `main/display/ui/widgets/control_center.cc` | 整个 `main/display/ui/` 官方无 |
| 教育卡 + 写字笔画 GIF（拼音/组词/释义两行屏显 + FontGif） | 🆕 | `ui_display.cc`、`education_mcp_tools.cc:39` | 教育产品特性 |
| `text_font.h` 字体代理（RAM 值拷贝 + PSRAM cbin 兜底字体解决缺字） | 🆕 | `main/display/text_font.h` | 官方无 |
| 4G PPP 优雅释放（CFUN=4）/ 双网切换语音编排 / WiFi 模式 ML307 飞行模式省电 | 🆕 | board `:568/:1124/:918-936` | 板级编排，非协议栈 |
| ML307 基类（上电 8s 时序 / CSQ 分档 / Cached 取值防阻塞） | 🔧 | `main/boards/common/ml307_board.cc` | 官方有基类，我们改时序 |
| `wifi_board.cc`（蓝牙+热点双配网 + 切换编排，757 行 diff） | 🔧 | `main/boards/common/wifi_board.cc` | 官方基础上大改 |

**📦 仅 vendor（未改源码，非自研）**：`78__esp-ml307`（官方 4G 库，官方也支持 4G）、`78__xiaozhi-fonts`、`mydazy/esp_lcd_jd9853`（官方 Waveshare 驱动发布版）、`espressif__esp-sr`（官方语音库）。

---

## 二、应用 / MCP / 业务模块

### 业务模块（官方 main/ 全无这些文件）

| 模块 | 类型 | 代码位置 | 核心能力 |
|---|---|---|---|
| **闹钟 AlarmManager** | 🆕 | `main/alarm_manager.cc`（659 行） | 8 槽位 CRUD / 一次性+按周重复 / 深睡 RTC 定时唤醒（按剩余时长分段睡）/ NVS 持久化 / 未校时防触发 |
| **番茄钟 PomodoroManager** | 🆕 | `main/pomodoro_manager.cc`（221 行） | 1Hz 倒计时三态机 / 触屏统一暂停入口 / 到 0 播提示+AI 鼓励 / 运行期抑制休眠 |
| **流程引擎 FlowEngine**（原"直播伴侣"） | 🆕 | `main/flow_engine.cc`（20KB） | **云端 JSON 脚本顺序播放器**（kTts/kTtai/kSet 三步骤）/ 被语音打断自动 Suspend / 延时自动 Resume 并口播"刚才讲到…继续" |
| **教育场景池 EduScenePool** | 🆕 ⚠️ | `main/edu_scene_pool.cc`（4.9KB） | 10 槽"摇一摇启蒙主题"+调用计数 / 写侧（云端 edu_pool 下发）活，**读侧已断**（摇一摇→AI 被砍） |
| **远程命令 RemoteCmd**（遥控总线） | 🆕 | `main/remote_cmd.cc`（489 行） | 复用对话通道做远程遥控，**19 个命令**（reboot/ota/sleep/tts/ttai/volume/gain/flow/music_*/edu_pool/update_prompt/wakeword…）；⚠️**无任何鉴权** |
| **MP3 音乐播放 MusicPlayer** | 🆕 | `main/audio/music_player.cc`（183 行） | 仅流式 HTTP MP3 / 乐鑫 esp_audio_codec / 三任务流水线 PSRAM / **与对话流互斥的第二音频链路** |
| **闹铃响铃器 AlarmRinger** | 🆕 ⚠️ | `main/audio/alarm_ringer.cc`（203 行） | kAlarm 必醒（60s/12 步，震动+AI 播报+5min 兜底）；kReminder 温和模式**未接线** |
| 类型维度 SceneType / ActivityType + 状态事件总线 DeviceStateEventManager | 🆕 | `scene_type.h`、`activity_type.h`、`device_state_event.cc` | 供 FlowEngine 感知 TTS 播完 |

### MCP 对外能力（新增 16 个工具，删了官方 camera.take_photo / screen.snapshot）

| 工具 | 代码位置 | 作用 |
|---|---|---|
| `self.get_mac_address` | `mcp_server.cc:51` | 返回 MAC |
| `self.audio.set_aec` | `:67` | 开关说话打断（AEC）|
| `self.audio.set_stt_popup` / `get_stt_popup` | `:79/:91` | 识别确认提示音开关/查询（NVS）|
| `self.power.set_sleep_mode` / `get_sleep_mode` | `:112/:124` | 休眠开关/查询（关→常亮不深睡）|
| `self.music.play` / `stop` | `:142/:194` | 流式 MP3 播放/停止 |
| `self.alarm.add/remove/list/dismiss` | `alarm_manager.cc:384` | 闹钟增删查 + 关响铃 |
| `self.pomodoro.start/pause/stop/status` | `pomodoro_manager.cc:159` | 番茄钟控制 |

### application / 状态机 应用层扩展（application.cc 1567 行 vs 官方 1131）

| 扩展 | 类型 | 代码位置 | 说明 |
|---|---|---|---|
| 文本驱动对话原语（SendTextToTts/SendTextToAI/**UpdateSystemPrompt**/ScheduleDelayedWake） | 🆕 | `application.cc:1282-1332` | UpdateSystemPrompt 是教育卡核心：运行期换 AI 人设 |
| `custom` 消息语义重写（官方"显示文本"→我们"RemoteCmd 命令路由"） | 🔧 | `application.cc:863` | |
| 教育卡 `[主_辅]` 标记提取（每句 TTS 解析 `[苹果_píng guǒ]`→显示教育卡） | 🆕 | `application.cc:800-813` | |
| stt 文本→识别确认提示音（OGG_POPUP 三重门控） | 🆕 | `application.cc:815` | |
| 弱网三态自动重连（区分用户退/服务器告别/真弱网，退避 3 次+5min 限流） | 🔧 | `application.cc:706-771` | |
| 音频发送熔断（连续 100 帧失败退对话） | 🆕 | `application.cc:377` | |
| 多协议自动选型（百度 bcelive / joyinside / 默认） | 🔧 | `application.cc:667` | 官方只 MQTT+通用 WS |
| 休眠判据扩展（加 MusicPlaying + 番茄钟 active） | 🔧 | `application.cc:1483-1490` | |
| OTA 加 mydazy 云 `/status` 上报 + `/switch` 切换通道（764 行 vs 官方 492） | 🆕 | `ota.cc:714/:328/:271` | RequestSwitch（NFC/iBeacon）⚠️未接线 |
| settings 加 GetFloat/SetFloat（×1000 存 int32，用于 Vref 校准） | 🔧 | `settings.cc:91` | |

---

## 三、⚠️ 技术债 / 已就位未接线点（开发核对重点）

| 项 | 现状 | 位置 |
|---|---|---|
| EduScenePool 读侧 | `GetRandomWithCount()` 全仓无调用方（摇一摇→AI 已砍，现只用于摇停闹钟） | `edu_scene_pool.cc` |
| AlarmRinger kReminder 模式 | 应用层无调用方 | `alarm_ringer.cc` |
| Ota::RequestSwitch（NFC/iBeacon） | 定义完整，board 未调用 | `ota.cc:328` |
| SceneType kTodo / kRoleSwitcher | 枚举占位未实现 | `scene_type.h` |
| 控制中心呼出 | 实为「单击状态栏 y<36」，**下滑已废弃但注释仍写"下滑唤起"** | `mydazy_p30_board.cc:384` |
| 开机 logo 渐出 `FinishBootAndShowClock()` | 死代码（无调用方），实际硬切时钟 | 显示层 |
| 陀螺仪拿起唤醒 | 目前播欢迎音+联网（非静默，扰民），且运行期默认关（NVS pickupWake=0） | board |
| 省电一级 | 实际只息屏降亮、**不降频不关音频**（cpu_max_freq=120 不满足 <120 分支） | `power_save_timer` |
| 桌面双击 strike | `sc7a20h_strike` 已写未启用 | `sc7a20h.c` |
| RemoteCmd 鉴权 | 19 命令遥控**无签名/token/设备身份**，信任边界=已建连接 | `remote_cmd.cc` |

---

## 四、统计汇总

- **纯新增板型**：2（P30-4G / P30-WiFi）
- **纯新增自研驱动**：5（SC7A20H / MP3 player / I2C worker / AXS5106L 触摸全套 / codec 转发）
- **纯新增业务模块**：7（闹钟 / 番茄钟 / 流程引擎 / 教育场景池 / 远程命令 / 音乐播放 / 闹铃）
- **MCP 新增工具**：16（删官方 camera/snapshot 2 个）
- **自研 UI 体系**：UiDisplay + 控制中心 + 教育卡/笔画 + 字体代理（`main/display/ui/` 整目录官方无）
- **改动官方核心**：board.h / ml307_board / power_manager / power_save_timer / wifi_board / application.cc / ota.cc / mcp_server.cc
- **技术债/未接线点**：10（见第三节，核对重点）
